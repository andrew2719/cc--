#include<bits/stdc++.h>

using namespace std;

int main()
{
    queue<int> q;
    // input n eelements
    int n;
    cin>>n;
    for(int i=0;i<n;i++)
    {
        int x;
        cin>>x;
        q.push(x);
    }

    // read a value k - no of sub arrays to be formed
    int k;
    cin>>k;

    // find max sum with total combinations of k elements in the queue
    
    

    
}